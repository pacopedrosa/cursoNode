// Hacer crear reserva, getAllReservas, getReservaById, deleteReservaById y updateReserva

import db from "./database.js"

export const createReseva = (idCliente, fechaEntrada, fechaSalida) => {
    const sql = `INSERT INTO reservas ()`
}